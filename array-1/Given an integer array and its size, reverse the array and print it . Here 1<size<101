#include<iostream>
using namespace std;
int main(){
    int shaun[]={1,3,5,2,6,9,33};
    int size = sizeof(shaun)/sizeof(shaun[0]);
    for(int i=size-1;i>=0;i--){
        cout<<shaun[i]<<" ";
    }
    return 0;
}
